server:
    port: 8080

spring:
    datasource:
        driver-class-name: org.postgresql.Driver
        url: ${url}
        username: ${username}
        password: ${password}
    jpa:
        database: postgresql
        database-platform: org.hibernate.dialect.PostgreSQLDialect
        properties:
            hibernate:
                default_schema: prod
